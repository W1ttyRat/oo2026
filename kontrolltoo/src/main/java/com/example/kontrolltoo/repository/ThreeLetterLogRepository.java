package com.example.kontrolltoo.repository;

import com.example.kontrolltoo.entity.ThreeLetterLog;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ThreeLetterLogRepository extends JpaRepository<ThreeLetterLog, Long> {
}
